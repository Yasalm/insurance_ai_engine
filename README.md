| Eval type (what you measure)    | Metric name / module type inevaluate     | What it tells you for OCR                                                                         | Typical input (forcompute)                   | When to use                                                                    |
| ------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------ |
| Character Error Rate (CER)      | cer(metric)                              | How many characters are wrong vs ground truth, normalized by total chars. Lower is better.        | predictions=[pred_strs],references=[gt_strs] | Core metric for text OCR, especially line/paragraph recognition.               |
| Word Error Rate (WER)           | wer(metric)                              | How many words are wrong (insertions, deletions, substitutions) vs ground truth. Lower is better. | predictions=[pred_strs],references=[gt_strs] | When your OCR output is word‑based (documents, subtitles, etc.).               |
| Sequence accuracy / exact match | accuracyorexact_match(metric)            | Fraction of lines where theentirepredicted string equals GT.                                      | predictions=[pred_strs],references=[gt_strs] | For strict evaluation: “How many lines are perfectly recognized?”              |
| Edit distance score             | edit_distance(metric) or custom          | Raw Levenshtein distance between predicted and true strings.                                      | predictions=[pred_strs],references=[gt_strs] | When you want raw distance instead of normalized CER/WER.                      |
| Combined OCR metrics            | evaluate.combine([...])(metrics)         | Runs CER, WER, accuracy, etc. together.                                                           | Same as above; sharedpredictions,references. | To get a more complete OCR eval in one call.                                   |
| Model A vs B on CER/WER         | mcnemar,exact_match(comparison)          | Checks if differences between two models are statistically significant.                           | predictions_a,predictions_b,references       | When you want to say “Model B is significantly better than A on this dataset”. |
| Dataset text length stats       | word_length,element_count(measurement)   | How long the lines are (chars/words), count of samples, etc.                                      | data=[gt_strs]or dataset column              | To understand if your dataset matches your production text (short/long, etc.). |
| Character / token distribution  | custom measurement (e.g. char frequency) | What characters/scripts appear (Arabic, digits, symbols).                                         | Dataset column as input                      | To see if dataset covers the alphabets/symbols your OCR must handle.           |**| Eval type (what you measure)    | Metric name / module type inevaluate     | What it tells you for OCR                                                                         | Typical input (forcompute)                   | When to use                                                                    |
| ------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------ |
| Character Error Rate (CER)      | cer(metric)                              | How many characters are wrong vs ground truth, normalized by total chars. Lower is better.        | predictions=[pred_strs],references=[gt_strs] | Core metric for text OCR, especially line/paragraph recognition.               |
| Word Error Rate (WER)           | wer(metric)                              | How many words are wrong (insertions, deletions, substitutions) vs ground truth. Lower is better. | predictions=[pred_strs],references=[gt_strs] | When your OCR output is word‑based (documents, subtitles, etc.).               |
| Sequence accuracy / exact match | accuracyorexact_match(metric)            | Fraction of lines where theentirepredicted string equals GT.                                      | predictions=[pred_strs],references=[gt_strs] | For strict evaluation: “How many lines are perfectly recognized?”              |
| Edit distance score             | edit_distance(metric) or custom          | Raw Levenshtein distance between predicted and true strings.                                      | predictions=[pred_strs],references=[gt_strs] | When you want raw distance instead of normalized CER/WER.                      |
| Combined OCR metrics            | evaluate.combine([...])(metrics)         | Runs CER, WER, accuracy, etc. together.                                                           | Same as above; sharedpredictions,references. | To get a more complete OCR eval in one call.                                   |
| Model A vs B on CER/WER         | mcnemar,exact_match(comparison)          | Checks if differences between two models are statistically significant.                           | predictions_a,predictions_b,references       | When you want to say “Model B is significantly better than A on this dataset”. |
| Dataset text length stats       | word_length,element_count(measurement)   | How long the lines are (chars/words), count of samples, etc.                                      | data=[gt_strs]or dataset column              | To understand if your dataset matches your production text (short/long, etc.). |
| Character / token distribution  | custom measurement (e.g. char frequency) | What characters/scripts appear (Arabic, digits, symbols).                                         | Dataset column as input                      | To see if dataset covers the alphabets/symbols your OCR must handle.           || Eval type (what you measure)    | Metric name / module type inevaluate     | What it tells you for OCR                                                                         | Typical input (forcompute)                   | When to use                                                                    |
| ------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------ |
| Character Error Rate (CER)      | cer(metric)                              | How many characters are wrong vs ground truth, normalized by total chars. Lower is better.        | predictions=[pred_strs],references=[gt_strs] | Core metric for text OCR, especially line/paragraph recognition.               |
| Word Error Rate (WER)           | wer(metric)                              | How many words are wrong (insertions, deletions, substitutions) vs ground truth. Lower is better. | predictions=[pred_strs],references=[gt_strs] | When your OCR output is word‑based (documents, subtitles, etc.).               |
| Sequence accuracy / exact match | accuracyorexact_match(metric)            | Fraction of lines where theentirepredicted string equals GT.                                      | predictions=[pred_strs],references=[gt_strs] | For strict evaluation: “How many lines are perfectly recognized?”              |
| Edit distance score             | edit_distance(metric) or custom          | Raw Levenshtein distance between predicted and true strings.                                      | predictions=[pred_strs],references=[gt_strs] | When you want raw distance instead of normalized CER/WER.                      |
| Combined OCR metrics            | evaluate.combine([...])(metrics)         | Runs CER, WER, accuracy, etc. together.                                                           | Same as above; sharedpredictions,references. | To get a more complete OCR eval in one call.                                   |
| Model A vs B on CER/WER         | mcnemar,exact_match(comparison)          | Checks if differences between two models are statistically significant.                           | predictions_a,predictions_b,references       | When you want to say “Model B is significantly better than A on this dataset”. |
| Dataset text length stats       | word_length,element_count(measurement)   | How long the lines are (chars/words), count of samples, etc.                                      | data=[gt_strs]or dataset column              | To understand if your dataset matches your production text (short/long, etc.). |
| Character / token distribution  | custom measurement (e.g. char frequency) | What characters/scripts appear (Arabic, digits, symbols).                                         | Dataset column as input                      | To see if dataset covers the alphabets/symbols your OCR must handle.           |**